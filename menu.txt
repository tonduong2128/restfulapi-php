1 : Connect database PDO | Restful API PHP
    1. Cấu trúc thư mục 
        1. api (controller)
        2. config
        2. model // dùng để kết nối với database
        3. view
    2. MySQL
        1. mysql (không còn được sử dụng nữa) 
        2. mysqli (chỉ đáp ứng được những kểu dữ liệu của mysqli (linux))
        3. PDO  (support tới 12 kết nối dữ liệu khác nhau)
    3. CRUD
2 : Read API | Restful API PHP
    $query ="SELECT* FROM tbl_questions as c order by c.id";
    $kiểu object in php
        $question_item = [
                'id' => $id,
                'a' => $a,
                'b' => $b,
                'c' => $c,
                'd' => $d,
                'answer' => $answer
            ];
    1 kểu query mới
    1. header("Access-Control-Allow-Origin: *");
        Cho phép trang nào lấy dữ liệu. // *:tất cả
    2. header("Content-type: aplication/json");
        Kểu trả về
3 : Show API | Restful API PHP

4 : Create API | Restful API PHP
    1. $data = json_decode(file_get_contents('php://input')); //lấy dữ liệu từ json gửi lên còn qua form là dùng $_POST
    2. $query = "INSERT INTO tbl_questions SET title=:title, a=:a, b=:b, c=:c, d=:d, answer=:answer"; //trong PDO viết z khác với pdo
5 : Update và delete dữ liệu | Restful API PHP
    1. PUT